[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning:streamlit
    ignore::PendingDeprecationWarning
markers =
    integration: 需要真实外部 API 的集成测试（默认跳过）